#!/bin/bash
claude -p "Run python ~/.claude/fetch-docs.py --check to check if the Claude Code docs have updated. If there are updates, run python ~/.claude/fetch-docs.py --update to pull them down. If everything is up to date, just say so and exit."
